name: Build Game
on: push
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: krdlab/setup-haxe@v1
        with:
          haxe-version: 4.3.7
      - name: Cache Haxe Compiler Generation (.haxecache)
        uses: actions/cache@v4
        with:
          path: .haxecache
          key: ${{ runner.os }}-haxe-generation-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-haxe-generation-

    # 2. 오직 이 빌드 스텝 안에서만 캐시 환경 변수와 플래그가 작동하도록 묶기
      - name: Install Libraries
        run: |
          haxelib install lime 8.1.2
          haxelib install openfl 9.3.3
          haxelib install flixel 5.6.1
          haxelib install flixel-addons 3.2.2
          haxelib install flixel-ui 2.6.1
          haxelib install flixel-tools
          haxelib install hscript 1.1.3
          haxelib install hxdiscord_rpc 1.2.4
          haxelib install flxanimate
          haxelib install hxcpp
          haxelib install hscript-iris 1.1.3
          haxelib install tjson 1.4.0
          haxelib git linc_luajit https://github.com/superpowers04/linc_luajit
          haxelib install hxdiscord_rpc
          haxelib git discord_rpc https://github.com/Aidan63/linc_discord-rpc
          haxelib git linc_luajit https://github.com/nebulazorua/linc_luajit.git
          haxelib run lime setup
      - name: Build Psych Engine
        env:
          HAXE_CACHE: .haxecache
          HAXE_COMPILER_CACHE: .haxecache
        run: |
          haxelib run lime build windows -D haxe-cache=.haxecache
        
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: FNF-BattleCats-Build
          path: export/release/windows/bin
